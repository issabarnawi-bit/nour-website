"use client";
import { useEffect,useMemo,useState } from "react";
import { motion } from "framer-motion";
import { createClient } from "../../../src/lib/supabase/client";

type Props={language:"ar"|"en"};
type Value={enabled?:boolean;name_ar?:string;name_en?:string;title_ar?:string;title_en?:string;message_ar?:string;message_en?:string;image_media_id?:string|null};
export default function CeoMessage({language}:Props){const supabase=useMemo(()=>createClient(),[]);const[value,setValue]=useState<Value|null>(null);const[image,setImage]=useState("");useEffect(()=>{void(async()=>{const{data}=await supabase.rpc("get_public_platform_settings");const row=(data||[]).find((x:{setting_key:string})=>x.setting_key==="home.ceo_message");if(!row)return;const v=row.value_json as Value;setValue(v);if(v.image_media_id){const{data:m}=await supabase.from("media").select("bucket,path").eq("id",v.image_media_id).maybeSingle();if(m)setImage(supabase.storage.from(m.bucket).getPublicUrl(m.path).data.publicUrl)}})()},[supabase]);if(!value?.enabled)return null;const ar=language==="ar";const name=ar?value.name_ar:value.name_en;const title=ar?value.title_ar:value.title_en;const message=ar?value.message_ar:value.message_en;return <section className="nr-ceo" dir={ar?"rtl":"ltr"}><div className="nr-container nr-ceo-grid"><motion.div className="nr-ceo-photo" initial={{opacity:0,y:24}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.25}}>{image?<img src={image} alt={name||title||"CEO"}/>:<div className="nr-ceo-placeholder">NourApp</div>}</motion.div><motion.div className="nr-ceo-copy" initial={{opacity:0,y:28}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.25}} transition={{delay:.08}}><span className="nr-kicker">{ar?"كلمة الرئيس التنفيذي":"CEO MESSAGE"}</span><h2>{ar?"نبني تجربة عمرة أكثر سهولة وطمأنينة":"Building a simpler, more reassuring Umrah journey"}</h2><blockquote>{message}</blockquote><div className="nr-ceo-signature"><strong>{name}</strong><span>{title}</span></div></motion.div></div><style jsx global>{`.nr-ceo{padding:100px 0;background:linear-gradient(145deg,var(--nr-soft),var(--nr-bg));overflow:hidden}.nr-ceo-grid{display:grid;grid-template-columns:minmax(280px,.78fr) minmax(0,1.22fr);gap:70px;align-items:center}.nr-ceo-photo{position:relative;min-height:480px;border-radius:32px;overflow:hidden;background:linear-gradient(145deg,var(--nr-blue-dark),var(--nr-blue));box-shadow:0 30px 70px rgba(16,44,82,.18)}.nr-ceo-photo img{width:100%;height:100%;position:absolute;inset:0;object-fit:cover}.nr-ceo-placeholder{min-height:480px;display:grid;place-items:center;color:white;font-size:34px;font-weight:900}.nr-ceo-copy h2{font-size:clamp(32px,4vw,54px);line-height:1.25;margin:18px 0 28px;max-width:820px}.nr-ceo-copy blockquote{margin:0;padding:0;font-size:clamp(18px,1.7vw,23px);line-height:2;color:var(--nr-muted);white-space:pre-line}.nr-ceo-signature{margin-top:30px;padding-top:22px;border-top:1px solid color-mix(in srgb,var(--nr-text) 12%,transparent);display:grid;gap:4px}.nr-ceo-signature strong{font-size:20px}.nr-ceo-signature span{color:var(--nr-blue);font-weight:700}@media(max-width:800px){.nr-ceo{padding:72px 0}.nr-ceo-grid{grid-template-columns:1fr;gap:34px}.nr-ceo-photo,.nr-ceo-placeholder{min-height:380px}.nr-ceo-copy h2{font-size:34px}}`}</style></section>}
